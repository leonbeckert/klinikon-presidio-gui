# Klinikon Pseudonymisierer - Umgebungskonfiguration
# Kopiere diese Datei zu .env und passe die Werte an

# Logging-Level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# App-Umgebung: development, production
APP_ENV=production

# Basic Auth für Analyzer/Anonymizer APIs
# Format: username:$$apr1$$hashedpassword (note the double $$)
# Generate with: htpasswd -nb username password
# Then replace all $ with $$ for docker-compose
# Example: presidio:$$apr1$$bvKnGO/N$$IYaaTqXn592kbKP2xWeeB.
PRESIDIO_BASIC_AUTH=presidio:$$apr1$$bvKnGO/N$$IYaaTqXn592kbKP2xWeeB.

# Optional: Custom-Ports (Standard: siehe docker-compose.yml)
# ANALYZER_PORT=5002
# ANONYMIZER_PORT=5001
# UI_PORT=8501
